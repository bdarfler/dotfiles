#!/usr/bin/env bash

for i in ~/Dropbox/Podcasting/*.mp3
do
	# If the file is missing title tag
	if [ -z "$(id3v2 -l "$i" | egrep TI?T2)" ];
	then
		# Set the title tag to the filename
		id3v2 -2 --TIT2 "${i::-4}" "$i"
	fi
done
